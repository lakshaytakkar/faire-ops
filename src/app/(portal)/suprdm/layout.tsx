export const metadata = {
  title: "SuprDM Admin | Suprans",
  description: "Admin for SuprDM — users, plans, features, automations, payments.",
}

export default function SuprdmLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
