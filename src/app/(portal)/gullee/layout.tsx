export const metadata = {
  title: "Gullee Admin | Suprans",
  description: "Admin for Gullee B2B pet wholesale — products, retailers, orders, applications.",
}

export default function GulleeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
