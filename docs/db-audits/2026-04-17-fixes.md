# DB hygiene fixes — applied 2026-04-17

Report companion to `2026-04-17.md`. Tracks what ran, what was skipped (and why), and the deferred list.

## Applied

### Phase A — safe additive (migration `db_hygiene_2026_04_17_phase_a_indexes_columns_dups`)
- **13 FK indexes** created on hot tables (>5k rows): `usdrop.user_activity_log.user_id`, `public.bank_transactions_v2.{account_id,matched_vendor_id}`, `ets.products.{category_l3_id,vendor_id}`, `usdrop.products.{category_id,subcategory_id,supplier_id}`, `ets.pos_sale_items.{product_id,sale_id}`, `ets.pos_stock_movements.{product_id,store_id}`, `public.calls.user_phone_id`.
- **18 hot-filter indexes** (created_at / updated_at / email / status) on the 18 >1k-row tables flagged in §3.2.
- **9 `created_at timestamptz DEFAULT now()`** columns added to active tables that were missing timestamps (`ets.pos_sale_items`, `ets.collection_items`, `ets.pos_inventory`, `ets.pos_register_sessions`, `ets.import_batches`, `ets.store_staff`, `public.chat_channel_members`, `public.orphan_recordings`, `usdrop.user_content_access`).
- **9 duplicate indexes** dropped (`b2b`, `b2b_ecosystem`, `jsblueridge`, `hq`, `public` — each pair was byte-identical per `pg_get_indexdef`).

### Phase B — FK constraints (migration `db_hygiene_2026_04_17_phase_b_c_fks_and_checks`)
Applied to every column probed with **0 orphans**:
- `public.chat_channel_members.channel_id` → `public.chat_channels(id)` ON DELETE CASCADE
- `public.chat_channel_reads.user_id` → `public.users(id)` ON DELETE CASCADE
- `public.chat_dm_reads.user_id` → `public.users(id)` ON DELETE CASCADE
- `public.chat_messages.channel_id` → `public.chat_channels(id)` ON DELETE CASCADE
- `public.task_comments.task_id` → `public.tasks(id)` ON DELETE CASCADE
- `public.task_links.task_id` → `public.tasks(id)` ON DELETE CASCADE
- `public.task_subtasks.task_id` → `public.tasks(id)` ON DELETE CASCADE
- `ets.stores.client_id` → `ets.clients(id)` ON DELETE SET NULL
- `hq.departments.head_employee_id` → `hq.employees(id)` ON DELETE SET NULL
- `usdrop.categories.parent_category_id` → `usdrop.categories(id)` ON DELETE SET NULL
- `usdrop.dev_tasks.project_id` → `public.projects(id)` ON DELETE CASCADE

Added **NOT VALID** (future writes enforced, historical rows not rejected):
- `ets.products.category_id` — 1 orphan row detected. Fix the orphan then `ALTER TABLE ets.products VALIDATE CONSTRAINT products_category_id_fkey`.

### Phase C — status CHECK constraints
Proposed enum sets were **widened** to match the values actually present in data (audit's proposed set was a guess; production data had extra values that would have failed the check). Applied:
- `ets.orders.status` — `('draft','submitted','paid','partially_paid','fulfilled','delivered','in_transit','cancelled','refunded')` — added `delivered`, `in_transit` (present in rows).
- `ets.pos_sales.status` — `('draft','completed','voided','refunded')`.
- `ets.stores.status` — `('planning','under_construction','finalised','live','paused','closed')` — added `finalised`.
- `hq.employees.status` — `('active','inactive','on_leave','notice_period','resigned','terminated')` — added `notice_period`, `resigned`.
- `usdrop.profiles.status` — `('new','onboarded','active','cancelled','churned')`.
- `usdrop.roadmap_progress.status` — `('not_started','in_progress','completed','skipped')`.

## Skipped (reason: would reject existing data)

| target | reason | next step |
|---|---|---|
| `public.chat_channel_members.user_id` FK → `public.users(id)` | **164 orphan rows** — more orphans than live users. Data is dirty. | Investigate why 164 chat memberships reference dropped/missing users; delete orphans OR merge in user records; then add FK. |

## Deferred (intentionally not this pass, per scope)

### Three-schema tenant model — RESPECTED
Per user direction:
- `b2b` = Faire wholesale admin (canonical main pipeline)
- `jsblueridge` = separate brand space, intentionally isolated for independent view/control
- `b2b_ecosystem` = separate spectrum for websites outside Faire (brand sites)

These are **intentional tenant boundaries**, not duplication. No DROP SCHEMA, no cross-schema data migration. §4.1 of the audit is **closed as wontfix**.

### Identity ambiguity (audit §2.3)
Requires a design session before any DDL:
- `public.users.employee_id` is `text`, not `uuid` — one-time data reconcile needed.
- `user_id` / `employee_id` / `client_id` FK to different targets across schemas — acceptable under the current schema-isolation model; revisit only if we consolidate identity.

### Table consolidation (audit §4.2)
- `ets.users` (0 rows) vs `public.users` (34) — drop `ets.users` after re-pointing `ets.store_staff.user_id` FK. Needs a portal-code grep first to confirm no imports.
- Empty `ets.{tickets,ticket_comments,dev_task_comments}` — similar: check code refs before drop.
- `usdrop.email_logs` (0 rows) / `public.sms_logs` (0 rows) / `chinaproducts.order_items` (0 rows) — drop candidates, same caveat.
- `hq.offices` + `suprans.offices` merge — needs row-level reconcile.
- `legal.documents` → `shared.documents` migration — needs row-level reconcile.

### Empty schemas `client` + `vendor`
Left in place. User said these are intentional stubs for future portal build. No action.

### RLS audit (§7 performance advisors: `multiple_permissive_policies`, `auth_rls_initplan`)
Out of scope for this agent. Queue a separate `rls-audit` agent in a future session.

### `unused_index` advisor findings (236)
Most are low-row tables where Postgres hasn't chosen the index yet. Re-run advisor after 30 days of production workload before dropping.

## Verification

```sql
-- Confirm new indexes exist
SELECT schemaname, tablename, indexname FROM pg_indexes
WHERE indexname LIKE 'idx_usdrop_ual_%'
   OR indexname LIKE 'idx_ets_products_%'
   OR indexname LIKE 'idx_public_btx2_%'
ORDER BY 1,2,3;

-- Confirm duplicate indexes gone
SELECT schemaname, indexname FROM pg_indexes
WHERE indexname IN ('idx_tracking_order','idx_tracking_status','idx_hq_emp_dept','idx_hq_emp_status','uniq_call_transcriptions_call_id');
-- expect: 0 rows

-- Confirm CHECKs reject bad values
INSERT INTO ets.orders (status) VALUES ('bogus');
-- expect: ERROR: new row violates check constraint "ets_orders_status_chk"
```
